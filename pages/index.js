export default function Home() {
  const btn = (bg) => ({
    background: bg,
    color: "#fff",
    borderRadius: 14,
    padding: "10px 16px",
    textDecoration: "none",
    display: "inline-block"
